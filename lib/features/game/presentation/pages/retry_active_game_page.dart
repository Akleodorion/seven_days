import 'package:flutter/material.dart';

class RetryActiveGamePage extends StatelessWidget {
  const RetryActiveGamePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
